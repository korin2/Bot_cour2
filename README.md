# Telegram Finance Bot

Бот для отслеживания финансовых данных с ИИ помощником.

## Функциональность

- 💱 Курсы валют ЦБ РФ с прогнозом на завтра
- ₿ Курсы криптовалют через CoinGecko API
- 💎 Ключевая ставка ЦБ РФ
- 🤖 Универсальный ИИ помощник (DeepSeek)
- 🔔 Уведомления о достижении курсов валют
- 🌅 Ежедневная рассылка основных данных

## Полная структура проекта

```
telegram-finance-bot/
.
├── bot/
│   ├── __init__.py
│   ├── __main__.py
│   ├── main.py
│   ├── config.py
│   ├── db.py
│   ├── handlers/
│   │   ├── __init__.py
│   │   ├── commands.py
│   │   ├── currency.py
│   │   ├── crypto.py
│   │   ├── key_rate.py
│   │   ├── ai_chat.py
│   │   ├── alerts.py
│   │   ├── callbacks.py
│   │   ├── messages.py
│   │   └── keyboards.py
│   ├── services/
│   │   ├── __init__.py
│   │   ├── cbr_api.py
│   │   ├── coingecko_api.py      # Новый файл
│   │   └── deepseek_api.py       # Новый файл
│   ├── utils/
│   │   ├── __init__.py           # Новый файл
│   │   ├── formatters.py         # Новый файл
│   │   └── helpers.py            # Новый файл
│   └── jobs/
│       ├── __init__.py           # Новый файл
│       └── alerts.py
├── requirements.txt
├── start.sh
├── railpack.toml
├── setup.py
├── Procfile
├── runtime.txt
├── run.py
├── wsgi.py
├── .gitignore
└── README.md
```

## Детальное описание модулей

### Основные модули

- **bot/main.py** - Главный файл запуска, инициализирует приложение и настраивает все обработчики
- **bot/config.py** - Конфигурационные константы, настройки API, токены
- **db.py** - Функции для работы с базой данных PostgreSQL

### Обработчики (handlers)

- **commands.py** - Базовые команды бота (/start, /help, /stop)
- **currency.py** - Курсы валют ЦБ РФ с прогнозом на завтра
- **crypto.py** - Курсы криптовалют через CoinGecko API
- **key_rate.py** - Ключевая ставка ЦБ РФ
- **ai_chat.py** - Интеграция с DeepSeek AI для универсального помощника
- **alerts.py** - Система уведомлений о курсах валют
- **callbacks.py** - Обработчики inline-кнопок
- **messages.py** - Обработка текстовых сообщений
- **keyboards.py** - Генерация клавиатур для меню

### Сервисы (services)

- **cbr_api.py** - Работа с API Центрального Банка РФ
- **coingecko_api.py** - Интеграция с CoinGecko API для криптовалют
- **deepseek_api.py** - API для общения с ИИ DeepSeek

### Утилиты (utils)

- **formatters.py** - Форматирование сообщений для Telegram
- **helpers.py** - Вспомогательные функции (разбивка длинных сообщений и т.д.)

### Фоновые задачи (jobs)

- **alerts.py** - Проверка условий уведомлений и ежедневная рассылка

## Установка и запуск

### Локальная разработка

1. Клонируйте репозиторий
2. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```
3. Создайте файл `.env` в корне проекта с переменными окружения:
   ```
   TELEGRAM_BOT_TOKEN=your_bot_token
   DATABASE_URL=your_database_url
   TG_BOT_APIDEEPSEEK=your_deepseek_api_key  # опционально
   ```
4. Запустите бота:
   ```bash
   python bot/main.py
   ```
   или
   ```bash
   python -m bot
   ```

### Развертывание на Railpack

Проект готов к развертыванию на Railpack. Все необходимые конфигурационные файлы созданы.

Railpack автоматически определит проект как Python приложение и выполнит следующие шаги:

1. Установит зависимости из `requirements.txt`
2. Запустит скрипт `start.sh`

Убедитесь, что в настройках Railpack установлены следующие переменные окружения:

- `TELEGRAM_BOT_TOKEN` (обязательно)
- `DATABASE_URL` (обязательно)
- `TG_BOT_APIDEEPSEEK` (опционально)

### Альтернативные способы запуска

```bash
# Через run.py
python run.py

# Через скрипт запуска
chmod +x start.sh
./start.sh
```

## Команды бота

- `/start` - Главное меню
- `/help` - Справка по командам
- `/rates` или `/currency` - Курсы валют ЦБ РФ
- `/crypto` - Курсы криптовалют
- `/keyrate` - Ключевая ставка ЦБ РФ
- `/ai` - Чат с ИИ помощником
- `/alert USD RUB 80 above` - Установить уведомление (пример)
- `/myalerts` - Мои уведомления
- `/debug_alerts` - Отладка уведомлений

## Используемые технологии

- **python-telegram-bot** - Фреймворк для создания Telegram ботов
- **asyncpg** - Асинхронный PostgreSQL клиент
- **requests** - HTTP запросы к API
- **beautifulsoup4** и **lxml** - Парсинг HTML страниц ЦБ РФ
- **python-dotenv** - Загрузка переменных окружения

## Особенности реализации

### База данных
- Используется PostgreSQL с асинхронным доступом через asyncpg
- Таблицы: users (информация о пользователях), alerts (уведомления)
- Автоматическое создание таблиц при запуске

### Обработка ошибок
- Комплексная система логирования
- Грейсфул деградация при недоступности API
- Fallback данные для критически важной информации

### Модульность
- Четкое разделение ответственности между модулями
- Легкость добавления нового функционала
- Переиспользование кода

## Переменные окружения

| Переменная | Обязательность | Описание |
|------------|----------------|----------|
| `TELEGRAM_BOT_TOKEN` | ✅ | Токен бота от @BotFather |
| `DATABASE_URL` | ✅ | URL подключения к PostgreSQL |
| `TG_BOT_APIDEEPSEEK` | ❌ | API ключ для DeepSeek AI |

## Примечания

- Для работы с криптовалютами используется CoinGecko API (бесплатный план)
- ИИ помощник использует DeepSeek API (требуется API ключ)
- Данные ЦБ РФ получаются с официального сайта
- Рекомендуется использовать PostgreSQL для базы данных
- Бот поддерживает как вебхуки, так и лонгполлинг (в текущей реализации используется лонгполлинг)

## Разработка

Проект использует модульную структуру, что упрощает добавление нового функционала:

1. Новые команды добавляются в соответствующий файл в `handlers/`
2. Новые API сервисы добавляются в `services/`
3. Вспомогательные функции в `utils/`
4. Фоновые задачи в `jobs/`

Все модули инициализируются в `bot/main.py` через соответствующие функции setup.
